package com.bond.md3elauncher.emulator.arcade

import com.bond.md3elauncher.emulator.fc.InternalFcActivity

/**
 * Runs MAME 2003-Plus through the shared libretro host in its own process.
 */
class InternalArcadeActivity : InternalFcActivity()
