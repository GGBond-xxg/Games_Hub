package com.bond.md3elauncher.emulator.n64

import com.bond.md3elauncher.emulator.fc.InternalFcActivity

/**
 * Runs the shared libretro host in a dedicated N64 process.
 */
class InternalN64Activity : InternalFcActivity()
